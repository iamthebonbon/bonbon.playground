<!-- No requirement changes. This is a pure implementation refactor.
     All claude-invoke requirements and scenarios remain identical to openspec/specs/claude-invoke/spec.md.
     The Anthropic Java SDK satisfies the same contract: same HTTP semantics, same error codes, same JSON envelope. -->
